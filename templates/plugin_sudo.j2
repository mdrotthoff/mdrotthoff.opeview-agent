# Managed by Ansible
Cmnd_Alias  OPSPLUGIN_{{ plugin_item.name }} = {{ opsview_agent_plugin_local }}/{{ plugin_item.name }}
{% if plugin_item.sudo == 'user' %}
{{ opsview_agent_user }} ALL = NOPASSWD: OPSPLUGIN_{{ plugin_item.name }}
{% elif plugin_item.sudo == 'group' %}
{{ '%' }}{{ opsview_agent_group }} ALL = NOPASSWD: OPSPLUGIN_{{ plugin_item.name }}
{% else %}
# Undefined sudo type
{% endif %}
